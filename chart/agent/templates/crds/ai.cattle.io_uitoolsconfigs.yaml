---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: uitoolsconfigs.ai.cattle.io
spec:
  group: ai.cattle.io
  names:
    kind: UIToolsConfig
    listKind: UIToolsConfigList
    plural: uitoolsconfigs
    shortNames:
    - uits
    singular: uitoolsconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.tools | length
      name: Tool Count
      type: integer
    - jsonPath: .spec.config.enabled
      name: Enabled
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UIToolsConfig is the Schema for the uitoolsconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UIToolsConfigSpec defines the desired state of UIToolsConfig
            properties:
              config:
                description: Config contains spec-level configuration
                properties:
                  defaultValues:
                    description: Default values for user-editable config fields
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  enabled:
                    description: Whether this UIToolsConfig is enabled and should
                      be used by the agent
                    type: boolean
                  maxTools:
                    description: Maximum number of UI tools the LLM can select per
                      response (0 = unlimited)
                    format: int32
                    type: integer
                  revision:
                    description: Revision number of this UIToolsConfig (for change
                      detection)
                    format: int32
                    type: integer
                  systemPrompt:
                    description: System prompt for the AI agent to use when deciding
                      how to utilize the UI tools
                    type: string
                type: object
              tools:
                description: Tools is the list of UI tool definitions
                items:
                  description: UIToolDefinition defines a single UI tool
                  properties:
                    category:
                      description: Category classifies the tool type (e.g., code,
                        card, table)
                      type: string
                    defaultValues:
                      description: Default values for user-editable tool fields
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    description:
                      description: Description explains what this UI tool does
                      type: string
                    enabled:
                      description: Enabled indicates whether this UI tool is active
                      type: boolean
                    metadata:
                      description: Metadata describes tool capabilities and constraints
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    name:
                      description: Name of the UI tool
                      type: string
                    prompt:
                      description: Prompt is used by the LLM to understand when and
                        how to select this tool
                      type: string
                    revision:
                      description: Revision number of this UI tool definition (for
                        change detection)
                      format: int32
                      type: integer
                    schema:
                      description: Schema defines the input validation schema for
                        this tool (standard JSON Schema)
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  required:
                  - category
                  - description
                  - name
                  - prompt
                  - schema
                  type: object
                minItems: 1
                type: array
            required:
            - config
            - tools
            type: object
          status:
            description: UIToolsConfigStatus defines the observed state of UIToolsConfig
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the UIToolsConfig's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              phase:
                description: Phase represents the current phase of the UI tools configuration
                type: string
              toolCount:
                description: ToolCount is the number of tools currently loaded
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
