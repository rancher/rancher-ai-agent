---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: uitoolsconfigs.ai.cattle.io
spec:
  group: ai.cattle.io
  names:
    kind: UIToolsConfig
    listKind: UIToolsConfigList
    plural: uitoolsconfigs
    shortNames:
    - uits
    singular: uitoolsconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.tools | length
      name: ToolCount
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UIToolsConfig defines a user interface component that can be rendered by the agent frontend
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UIToolsConfigSpec defines the desired state of UITools
            properties:
              tools:
                description: List of UI tools
                type: array
                items:
                  description: A single UI tool definition
                  type: object
                  properties:
                    name:
                      description: Name of the UI tool
                      type: string
                    category:
                      description: Category classifies the tool type (e.g., code, card, table) and can be used for organizing tools in the UI
                      type: string
                    description:
                      description: Description explains what this UI tool does
                      type: string
                    prompt:
                      description: Prompt is used by the LLM to understand when and how to select this tool
                      type: string
                    enabled:
                      description: Whether this UI tool is enabled
                      type: boolean
                      default: true
                    metadata:
                      description: Metadata describes tool capabilities and constraints
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    schema:
                      description: Schema defines the input validation schema for this tool (standard JSON Schema)
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    revision:
                      description: Revision number of this UI tool definition (for change detection)
                      type: integer
                      default: 0
                    defaultValues:
                      description: Default values for user-editable tool fields
                      type: object
                      properties:
                        enabled:
                          description: Default enabled state for this tool
                          type: boolean
                  required:
                  - name
                  - category
                  - description
                  - prompt
                  - schema
              config:
                description: Spec-level configuration for UIToolsConfig
                type: object
                properties:
                  systemPrompt:
                    description: System prompt for the AI agent to use when deciding how to utilize the UI tools
                    type: string
                  enabled:
                    description: Whether this UIToolsConfig is enabled and should be used by the agent
                    type: boolean
                    default: true
                  revision:
                    description: Revision number of this UIToolsConfig (for change detection)
                    type: integer
                    default: 0
                  maxTools:
                    description: Maximum number of UI tools the LLM can select per response (0 = unlimited)
                    type: integer
                    default: 5
                  defaultValues:
                    description: Default values for user-editable config fields
                    type: object
                    properties:
                      systemPrompt:
                        description: Default system prompt value
                        type: string
                      enabled:
                        description: Default enabled state
                        type: boolean
            required:
            - tools
            - config
            type: object
        type: object
    served: true
    storage: true
